const squareRegistry = new Map();

const chessTile = (x, y) => {
    const xPosition = x
    const yposition = y;
    let predecessor;

    const knightOffests = [
        [1,2], [1,-2],
        [2,1], [2,-1],
        [-1,2], [-1,-2],
        [-2,1], [-2,-1]
    ]
}